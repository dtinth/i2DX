
/**
 * @class i2DX.events.TouchHandler
 * A handler interface that can be used to handle touches on a particular component.
 */

/**
 * @method check
 * Checks whether a touch should be handled by this handler or not.
 * @param {i2DX.events.Touch} the touch that is made on the screen
 * @return {i2DX.events.TouchHandler.BoundHandler}
 */

/**
 * @class i2DX.events.TouchHandler.BoundHandler
 * A handler that is "bound" to a particular touch.
 */

/**
 * @method move
 * Fired when the finger has moved.
 * @param {i2DX.events.Touch} the touch that is made on the screen
 */

/**
 * @method release
 * Fired when the finger is released.
 */
